import 'package:flutter/material.dart';

/// Nothing to tell: a native app draws its own status bar, and the system colours it
/// from the theme rather than from a page.
void setPageColour(Color colour) {}
